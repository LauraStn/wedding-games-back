spring:
  jpa:
    show-sql: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  security:
    session-cookie:
      secure: false
      same-site: Lax

logging:
  level:
    com.weddinggames.backend: INFO
