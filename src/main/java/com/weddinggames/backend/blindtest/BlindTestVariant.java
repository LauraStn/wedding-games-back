package com.weddinggames.backend.blindtest;

public enum BlindTestVariant {
    /** The track is played at a reduced speed. */
    SLOWED_DOWN,
    /** The track is played backwards. */
    REVERSED,
    /** The track is played, then stopped mid-way for guests to continue the lyrics. */
    LYRICS_CONTINUATION
}
